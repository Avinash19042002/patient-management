apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: patient-management
spec:
  selector:
    app: api-gateway
  type: NodePort
  ports:
    - port: 4004
      targetPort: 4004
      nodePort: 30004
